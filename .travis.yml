language: node_js
node_js:
    - node
    - 22
cache: npm
install:
    - npm install
jobs:
    include:
        - stage: deploy
          if: branch == master && !fork
          node_js: 'node' # pre-installed version
          script:
              - npx semantic-release
